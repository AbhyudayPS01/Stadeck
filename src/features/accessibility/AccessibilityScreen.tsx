/** Accessibility — implements the challenge clause "accessibility". */
export default function AccessibilityScreen() {
  return (
    <main className="min-h-screen bg-fan-bg px-page py-section">
      <h1 className="font-display text-h1 text-fan-ink">Accessibility</h1>
      <p className="mt-2 text-body text-fan-muted">
        Accessible routing, seating info, and display controls for fans with access needs.
      </p>
    </main>
  );
}
