/** Transportation — implements the challenge clause "transportation". */
export default function TransportationScreen() {
  return (
    <main className="min-h-screen bg-fan-bg px-page py-section">
      <h1 className="font-display text-h1 text-fan-ink">Transportation</h1>
      <p className="mt-2 text-body text-fan-muted">
        Transit options and departure timing to get fans to and from MetLife Stadium.
      </p>
    </main>
  );
}
